> y

JS blockを追加して再レンダー。`style.setProperty(..., "important")` で Reveal.js のインライン max-height を上書き。レンダー成功。
> URL for Sant'Anna tool + accept changes

3 edits applied and re-rendered:
* RAG: Perplexity Sonar → Scite
* Self-Verification: LangChain self-reflection → Sant'Anna /verify-claims (1.6k forks)
* 出典実在確認: CrossRef Metadata Search → scite.ai
Render: success.
